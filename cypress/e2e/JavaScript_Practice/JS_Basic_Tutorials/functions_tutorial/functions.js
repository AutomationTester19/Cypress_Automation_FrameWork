function add(a,b){
  return a+b;
}

let sum = add(10,20);
console.log(sum);

// variables can be declare inside the function , not outside the function


function supply(a,b)
{
    const c = a*b;
    return c;
}
console.log(c); // to remove the error comment it

let mul = supply(10,20);
console.log(mul);